# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# the owned memory store (local file adapter) — never commit a person's life
/.trove-data/
*.local

# env / secrets — NEVER commit keys (this repo is published)
.env*
!.env.example

# misc
.DS_Store
*.pem
/coverage
npm-debug.log*
